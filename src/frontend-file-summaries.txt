{
  "frontend/src/App.tsx": "Main application component with authentication flow, profile setup dialog, case management UI, and search input that filters case cards by name, medical record number, owner, species, breed, sex, date of birth, and presenting complaint.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend operations including presentingComplaint parameter in createSurgeryCase calls, with server-only cases query using ['serverSurgeryCases'] key to prevent cache collision, and all mutations invalidating both keys to ensure proper refresh.",
  "frontend/src/types/cases.ts": "TypeScript type definitions for local surgery cases and form data structures, with arrivalDate and dateOfBirth now represented as YYYY-MM-DD strings for timezone-safe handling, and capturedImageUrl field added to both LocalSurgeryCase and CaseFormData for camera-captured reference images.",
  "frontend/src/utils/dates.ts": "Date utility functions with timezone-safe date-only handling for YYYY-MM-DD strings, including validation, conversion to/from nanoseconds, formatting for display, and conversion to/from Date objects for UI components.",
  "frontend/src/utils/localPersistence.ts": "Local storage utilities for offline case persistence with BigInt-safe JSON serialization, normalization that ensures presentingComplaint defaults to empty string for backward compatibility, and error throwing on save failures with verification of successful writes.",
  "frontend/src/utils/prefill.ts": "Medical record number matching and demographics prefill logic that extracts all required fields including dateOfBirth as a YYYY-MM-DD string from the most recent matching case.",
  "frontend/src/utils/csv.ts": "CSV parsing and serialization utilities with timezone-safe date-only handling for Arrival Date, using YYYY-MM-DD strings for export and import to prevent timezone shifts.",
  "frontend/src/hooks/useOnlineStatus.ts": "React hook that monitors browser online/offline status using native navigator events with proper initialization to null state for reliable distinction between true offline mode and backend connection failures.",
  "frontend/src/hooks/useSync.ts": "Manual sync hook that fetches current server state, prepares full union of cases for sync, pushes to backend, re-fetches, merges results preserving local-only fields including presentingComplaint, and updates both local storage and React Query cache with clear English error messages for authorization and network failures.",
  "frontend/src/hooks/useCasesStore.ts": "Unified cases store hook managing local and server state with race-safe mutations that normalize presentingComplaint to empty string when missing, persist to local storage first with error propagation, verify successful saves by reloading, then update React Query cache from the verified snapshot.",
  "frontend/src/components/layout/Header.tsx": "Application header with branding, environment label badge (Draft/Live) shown when authenticated, authentication button, and logout functionality that clears both query cache and local storage using the correct clearLocalCases function.",
  "frontend/src/components/layout/Footer.tsx": "Application footer with copyright notice and attribution link to caffeine.ai with proper UTM tracking parameters.",
  "frontend/src/components/cases/DateField.tsx": "Reusable date input component that works with YYYY-MM-DD strings, supporting both manual text entry and calendar picker selection with timezone-safe state management and proper null handling for the Calendar component.",
  "frontend/src/components/cases/CaseForm.tsx": "Comprehensive case creation and editing form with Presenting Complaint textarea field that persists to both local storage and backend, uses createTasksChecklistForNewCase for new cases and normalizeTasksChecklist for edits, and includes stabilized initialization logic with proper dependency management.",
  "frontend/src/components/cases/DemographicsQuickAdd.tsx": "Quick demographics input component with text area for pasting, camera capture dialog integration with error boundary protection for reference images, conditional rendering of CameraCapture only when dialog is open to prevent lifecycle issues, and preview display with remove/replace controls when an image is captured.",
  "frontend/src/components/cases/CameraCapture.tsx": "Camera preview and capture component using the prefabricated useCamera hook with defensive error handling via try/catch blocks around all camera operations, responsive sizing with explicit height, device-appropriate controls, mounted state tracking via useRef to prevent state updates after unmount, and callback support to return captured image as a data URL.",
  "frontend/src/components/cases/TasksChecklist.tsx": "Per-case tasks checklist component that normalizes incoming tasksChecklist and displays only required incomplete tasks, with updated empty state message \"All tasks completed\" when no tasks remain visible after completion.",
  "frontend/src/components/cases/CaseCard.tsx": "Enhanced card-style case display with timezone-safe date formatting, presenting complaint field with placeholder, and task completion flow that removes completed tasks from the visible list while persisting state to both local storage and backend.",
  "frontend/src/components/cases/CaseList.tsx": "Grid layout of case cards with loading states, enhanced empty state, client-side search filtering across multiple patient fields including presenting complaint with safe fallback for empty values, and sorted display based on user-selected criteria.",
  "frontend/src/components/cases/SortControls.tsx": "Sorting controls allowing users to select sort field (Arrival Date or Medical Record #) and toggle ascending/descending order.",
  "frontend/src/components/sync/SyncStatusBar.tsx": "Status bar displaying browser online/offline status and backend connection status with consistent icons, clear English labels, retry controls using the updated full-reset retry mechanism, and manual sync functionality.",
  "frontend/src/components/csv/CsvImportDialog.tsx": "CSV import dialog with file picker, preview, validation, and improved error handling that keeps the dialog open and usable on import failure while displaying user-facing error messages, with proper loading state during import.",
  "frontend/src/components/csv/CsvExportButton.tsx": "CSV export button generating downloads with only the two supported task columns (Discharge Notes, pDVM Notified) in the exported file.",
  "frontend/src/lib/utils.ts": "Utility function for merging Tailwind CSS classes using clsx and tailwind-merge for proper class name handling.",
  "frontend/src/index.css": "Global styles with OKLCH-based blue color system for light and dark modes, featuring accessible contrast ratios and cohesive design tokens.",
  "frontend/tailwind.config.js": "Tailwind configuration with extended blue-themed color palette, custom shadows, and animation utilities matching the app's design system.",
  "frontend/src/utils/tasksChecklist.ts": "Shared task definitions and utilities for the 7-task checklist system, with getRequiredTasks now filtering to show only required tasks that are NOT yet completed (hiding checked tasks from patient card display), while createTasksChecklistForNewCase ensures only form-selected tasks are marked required in new cases.",
  "frontend/src/utils/mergeCases.ts": "Utility functions for merging local and server cases by caseId, preserving local-only fields (demographicsRawText, capturedImageUrl, pendingSync) while ensuring presentingComplaint and other backend fields are properly merged, plus preparing full case unions for sync to prevent subset overwrites.",
  "frontend/src/hooks/useBackendConnection.ts": "Higher-level backend connection manager that composes actor initialization and backend probe, with improved retry mechanism that explicitly invalidates and refetches the probe query after actor retry to ensure full connection re-establishment.",
  "frontend/src/hooks/useEnvironmentLabel.ts": "Hook that derives environment label (Draft or Live) from the current URL hostname using heuristics for draft/staging/localhost detection.",
  "frontend/src/components/cases/EmptyCasesState.tsx": "Empty state component that conditionally renders migration guidance or simple \"No cases yet\" message with data source diagnostics, now guiding users to import CSV files instead of adding cases manually.",
  "frontend/src/components/startup/StartupRecoveryScreen.tsx": "Reusable recovery screen component that displays actionable connection error messages with context-specific troubleshooting guidance for token-related vs general connection failures.",
  "frontend/src/utils/draftAdminToken.ts": "Helper utility that retrieves the Draft admin token using URL/session persistence from urlParams.ts and provides safe checks for missing/empty tokens.",
  "frontend/src/utils/backendErrorMessages.ts": "Shared error classification utility that converts raw backend errors into user-facing English messages, distinguishing authorization, network, initialization, and unknown failures without mentioning Draft admin tokens for standard connection failures.",
  "frontend/src/hooks/useActor.ts": "Backend actor initialization hook that creates authenticated or anonymous actors based on identity state, with backend handling access control initialization automatically and no admin token coupling.",
  "frontend/src/hooks/useActorWithError.ts": "Enhanced wrapper around useActor that adds error tracking and retry functionality for actor initialization failures, with improved state tracking to reliably detect initialization failures and surface actionable error messages.",
  "frontend/src/components/errors/ModalErrorBoundary.tsx": "React error boundary component for modal content that catches rendering errors, displays a user-friendly fallback with close action, resets when the modal reopens via resetKey prop, and provides enhanced development-only logging with error details, component stack, reset key, debug label, and timestamp for easier debugging.",
  "frontend/DEPLOYMENT_NOTES.md": "Comprehensive deployment documentation with clean rebuild instructions, post-deploy verification checklist, and troubleshooting guidance for eliminating stale canister-id and binding artifacts.",
  "frontend/scripts/clean-rebuild-frontend.sh": "Bash script that performs deterministic frontend clean rebuild by removing stale artifacts, regenerating backend bindings, and rebuilding frontend in the correct order.",
  "frontend/scripts/verify-live-backend-binding.md": "Step-by-step verification guide for confirming the Live frontend uses fresh backend bindings and the currently deployed backend canister ID, with troubleshooting for common connection issues."
}